name: api
display-name: DevRev Beta REST API
error-discrimination:
  strategy: status-code
auth: auth-token
auth-schemes:
  auth-token:
    header: Authorization
    name: apiKey
    type: string
default-environment: default
environments:
  default: '{protocol}://{hostname}'
